"use client";

export function ConfiguratorDialog() {
  return (
    <div className="grid gap-3">
      {/* Theme configurator disabled (site is locked to Classic). */}
      {/* Keep other configurator sections here if/when you reintroduce them. */}
    </div>
  );
}
